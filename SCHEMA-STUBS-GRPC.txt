```
┌─────────────────────────────────────────────────────────────────────────────┐
│                    📦 MODULE COMMUN - Architecture gRPC                      │
└─────────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────────┐
│  1️⃣  FICHIERS SOURCE (.proto) - LES "INTERFACES"                            │
└─────────────────────────────────────────────────────────────────────────────┘

    commun/src/main/proto/
    │
    ├── hotel.proto ─────────────────────┐
    │   │                                 │  Définit le contrat gRPC
    │   service HotelService {           │  (équivalent d'une interface Java)
    │     rpc RechercherChambres(...)    │
    │     rpc EffectuerReservation(...)  │
    │   }                                 │
    │   message ChambreMessage {...}     │
    │                                     │
    └── agence.proto ────────────────────┘
        │
        service AgenceService {
          rpc Ping(...)
          rpc RechercherChambres(...)
        }

                        ⬇️  Maven Compilation
                        ⬇️  protobuf-maven-plugin
                        ⬇️

┌─────────────────────────────────────────────────────────────────────────────┐
│  2️⃣  CODE GÉNÉRÉ AUTOMATIQUEMENT - LES "STUBS"                              │
└─────────────────────────────────────────────────────────────────────────────┘

    commun/target/generated-sources/protobuf/
    │
    ├── grpc-java/                      ← SERVICES (stubs)
    │   └── org/tp1/commun/grpc/
    │       │
    │       ├── hotel/
    │       │   └── HotelServiceGrpc.java ★★★ STUB PRINCIPAL ★★★
    │       │       │
    │       │       ├── HotelServiceImplBase          [Serveur]
    │       │       │   └─→ extends BindableService
    │       │       │       └─→ Classe abstraite à implémenter
    │       │       │
    │       │       ├── HotelServiceBlockingStub      [Client SYNC]
    │       │       │   └─→ Appels synchrones (bloquants)
    │       │       │       └─→ rechercherChambres(request)
    │       │       │
    │       │       ├── HotelServiceStub              [Client ASYNC]
    │       │       │   └─→ Avec callbacks (StreamObserver)
    │       │       │
    │       │       └── HotelServiceFutureStub        [Client FUTURE]
    │       │           └─→ Retourne ListenableFuture<>
    │       │
    │       └── agence/
    │           └── AgenceServiceGrpc.java ★★★ STUB PRINCIPAL ★★★
    │               ├── AgenceServiceImplBase
    │               ├── AgenceServiceBlockingStub
    │               ├── AgenceServiceStub
    │               └── AgenceServiceFutureStub
    │
    └── java/                           ← MESSAGES (DTOs)
        └── org/tp1/commun/grpc/
            │
            ├── hotel/
            │   ├── ChambreMessage.java
            │   ├── RechercheRequest.java
            │   ├── RechercheResponse.java
            │   ├── ReservationRequest.java
            │   ├── ReservationResponse.java
            │   ├── HotelInfoRequest.java
            │   └── HotelInfoResponse.java
            │
            └── agence/
                ├── PingRequest.java
                ├── PingResponse.java
                └── AgenceReservationRequest.java

                        ⬇️  Maven Package
                        ⬇️

┌─────────────────────────────────────────────────────────────────────────────┐
│  3️⃣  JAR FINAL - Distribué aux autres modules                               │
└─────────────────────────────────────────────────────────────────────────────┘

    commun-0.0.1-SNAPSHOT.jar
    │
    ├── org/tp1/commun/grpc/hotel/
    │   ├── HotelServiceGrpc.class                    ← Stub compilé
    │   ├── HotelServiceGrpc$HotelServiceImplBase.class
    │   ├── HotelServiceGrpc$HotelServiceBlockingStub.class
    │   ├── HotelServiceGrpc$HotelServiceStub.class
    │   ├── HotelServiceGrpc$HotelServiceFutureStub.class
    │   ├── ChambreMessage.class
    │   ├── RechercheRequest.class
    │   └── ...
    │
    ├── org/tp1/commun/grpc/agence/
    │   ├── AgenceServiceGrpc.class
    │   └── ...
    │
    └── hotel.proto                                   ← Proto inclus
    └── agence.proto


┌─────────────────────────────────────────────────────────────────────────────┐
│  4️⃣  UTILISATION DANS LES AUTRES MODULES                                    │
└─────────────────────────────────────────────────────────────────────────────┘

╔═══════════════════════════════════════════════════════════════════════════╗
���  MODULE HOTELLERIE (Serveur gRPC)                                         ║
╚═══════════════════════════════════════════════════════════════════════════╝

    pom.xml
    ├── <dependency>
    │       <groupId>org.tp1</groupId>
    │       <artifactId>commun</artifactId>    ← Importe les stubs
    │   </dependency>
    │
    src/main/java/.../grpc/HotelGrpcService.java
    │
    │   @GrpcService
    │   public class HotelGrpcService
    │       extends HotelServiceGrpc.HotelServiceImplBase {  ← Utilise le stub
    │
    │       @Override
    │       public void rechercherChambres(
    │           RechercheRequest request,              ← Message généré
    │           StreamObserver<RechercheResponse> responseObserver) {
    │
    │           // Implémentation métier
    │           List<Chambre> chambres = hotelService.rechercherChambres(...);
    │
    │           // Convertir en message gRPC
    │           RechercheResponse response = RechercheResponse.newBuilder()
    │               .addAllChambres(...)
    │               .build();
    │
    │           responseObserver.onNext(response);
    │           responseObserver.onCompleted();
    │       }
    │   }


╔═══════════════════════════════════════════════════════════════════════════╗
║  MODULE AGENCE (Serveur + Client gRPC)                                    ║
╚═══════════════════════════════════════════════════════════════════════════╝

    [CÔTÉ SERVEUR]
    src/main/java/.../grpc/server/AgenceGrpcService.java
    │
    │   @GrpcService
    │   public class AgenceGrpcService
    │       extends AgenceServiceGrpc.AgenceServiceImplBase {  ← Stub serveur
    │
    │       @Override
    │       public void rechercherChambres(...) {
    │           // Agrège les résultats de plusieurs hôtels
    │       }
    │   }

    [CÔTÉ CLIENT]
    src/main/java/.../grpc/client/HotelGrpcClient.java
    │
    │   @Service
    │   public class HotelGrpcClient {
    │
    │       @GrpcClient("hotel-paris")
    │       private HotelServiceGrpc.HotelServiceBlockingStub hotelParisStub;  ← Stub client
    │
    │       public List<ChambreMessage> rechercherChambres(...) {
    │           // Appel gRPC synchrone
    │           RechercheResponse response = hotelParisStub.rechercherChambres(request);
    │           return response.getChambresList();
    │       }
    │   }


╔═══════════════════════════════════════════════════════════════════════════╗
║  MODULE CLIENT (Client gRPC uniquement)                                   ║
╚═══════════════════════════════════════════════════════════════════════════╝

    src/main/java/.../grpc/AgenceGrpcClient.java
    │
    │   @Service
    │   public class AgenceGrpcClient {
    │
    │       @GrpcClient("agence1")
    │       private AgenceServiceGrpc.AgenceServiceBlockingStub agence1Stub;  ← Stub client
    │
    │       public List<ChambreDTO> rechercherChambres(...) {
    │           // Convertir DTO → Proto
    │           RechercheRequest protoRequest = RechercheRequest.newBuilder()
    │               .setAdresse(...)
    │               .build();
    │
    │           // Appel gRPC
    │           RechercheResponse response = agence1Stub.rechercherChambres(protoRequest);
    │
    │           // Convertir Proto → DTO
    │           return response.getChambresList().stream()
    │               .map(this::convertToChambreDTO)
    │               .collect(Collectors.toList());
    │       }
    │   }


┌─────────────────────────────────────────────────────────────────────────────┐
│  📊 RÉCAPITULATIF                                                            │
└─────────────────────────────────────────────────────────────────────────────┘

┌────────────────┬──────────────────────────┬─────────────────────────────────┐
│ Fichier        │ Type                     │ Rôle                            │
├────────────────┼──────────────────────────┼─────────────────────────────────┤
│ hotel.proto    │ Définition IDL           │ Interface + Contrat gRPC        │
│ agence.proto   │ Définition IDL           │ Interface + Contrat gRPC        │
├────────────────┼──────────────────────────┼─────────────────────────────────┤
│ *ServiceGrpc   │ Stub généré              │ Code serveur + client           │
│ *ImplBase      │ Classe abstraite         │ À implémenter côté serveur      │
│ *BlockingStub  │ Client synchrone         │ Appels bloquants                │
│ *Stub          │ Client asynchrone        │ Appels avec callbacks           │
│ *FutureStub    │ Client avec Future       │ Appels non-bloquants            │
├────────────────┼──────────────────────────┼─────────────────────────────────┤
│ *Message       │ DTO généré               │ Objet de transfert de données   │
│ *Request       │ DTO généré               │ Paramètres d'entrée             │
│ *Response      │ DTO généré               │ Résultat de sortie              │
└────────────────┴──────────────────────────┴─────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────────┐
│  💡 POINTS CLÉS                                                              │
└─────────────────────────────────────────────────────────────────────────────┘

✅ Les fichiers .proto SONT les interfaces (contract-first design)
✅ Les stubs sont générés AUTOMATIQUEMENT lors de la compilation
✅ Un stub = Service complet (serveur + 3 types de clients)
✅ Les messages = DTOs type-safe générés automatiquement
✅ Tout est dans le JAR commun-0.0.1-SNAPSHOT.jar
✅ Les autres modules importent simplement ce JAR

┌─────────────────────────────────────────────────────────────────────────────┐
│  🔍 POUR VOIR LES STUBS GÉNÉRÉS                                              │
└─────────────────────────────────────────────────────────────────────────────┘

# Après compilation du module commun :
cd /home/corentinfay/Bureau/gRPCRepo/commun
mvn clean install

# Voir les sources Java générées :
tree target/generated-sources/protobuf/

# Voir les classes compilées dans le JAR :
jar tf target/commun-0.0.1-SNAPSHOT.jar | grep ServiceGrpc

# Dans votre IDE (IntelliJ/Eclipse) :
# Naviguer vers : commun/target/generated-sources/protobuf/grpc-java/
```

